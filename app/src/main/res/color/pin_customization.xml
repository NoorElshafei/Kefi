<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Use for the item to be input, set the value as the default to disable it -->
    <item android:color="@color/bink_dark" android:state_selected="true" />
    <item android:color="@color/bink_light" android:state_focused="true" />
    <item android:color="@color/bink_light" />
</selector>